// This is a Tailwind CSS configuration file. It specifies the content sources, theme customization, variants, and plugins for Tailwind CSS.
/** @type {import('tailwindcss').Config} */ 
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        poppins : ["var(--font-poppins)"],
      }
    },
  },
  variants: {
    extend: {},
  },
  plugins: [],
};
